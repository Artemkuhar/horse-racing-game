import type { ToastVariant } from '@/shared/ui/toast/model';

export const COLOR_BY_VARIANT: Record<ToastVariant, string> = {
  success: 'success',
  error: 'error',
  warning: 'warning',
  info: 'info',
};
